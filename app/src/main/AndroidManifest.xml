<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MascotaChat"
        android:theme="@style/Theme.MascotaChat">

        <!-- Actividad principal (Propietario) -->
        <activity android:name=".ChatUsuario"
            android:exported="true">  <!-- Añadir android:exported="true" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad del cuidador -->
        <activity android:name=".ChatCuidador"
            android:exported="true"></activity>

    </application>
</manifest>